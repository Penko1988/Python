divisor = int(input())
boundary = int(input())
max_multy = 0

for current_num in range(boundary, divisor, -1):
    if current_num % divisor == 0:
        max_multy = current_num
        break
print(max_multy)